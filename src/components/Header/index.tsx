import React from "react";
import { Container, Logo } from "./styles";

const Header = () => {
  return (
    <Container>
      <Logo to="dashboard">Audience Management Console</Logo>
    </Container>
  );
};
export default Header;
